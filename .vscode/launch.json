{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug middleware",
            "type": "go",
            "request": "launch",
            "mode": "test",
            "program": "${workspaceFolder}/tests",
        }
    ]
}